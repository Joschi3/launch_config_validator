name: Python Unit Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    container: ros:jazzy-ros-base
    steps:
      - name: Check out repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pip

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install --no-cache-dir  coverage
          python -m pip install . --break-system-packages


      - name: Run unit tests with coverage (always emit XML)
        env:
          PYTHONPATH: ${{ github.workspace }}
        run: |
          set +e
          python3 -m coverage run --source=launch_config_validator \
            -m unittest discover -s test -p "test_*.py" -t .
          status=$?
          # Always generate reports so Codecov has something to ingest
          python3 -m coverage xml -o coverage.xml || true
          python3 -m coverage report -m || true
          python3 -m coverage html || true
          exit $status

      - name: Upload coverage HTML as artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-html
          path: htmlcov

      - name: Upload coverage reports to Codecov
        if: always()
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
